services:
  slideo:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - slideo-data:/app/server/data
      - slideo-uploads:/app/server/uploads
    restart: unless-stopped

volumes:
  slideo-data:
  slideo-uploads:
